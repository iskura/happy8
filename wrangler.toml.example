# Cloudflare Pages 部署后，在控制台绑定 KV 命名空间到变量 LOTTERY_KV
# 同步接口 /api/kl8.txt 会把最新 txt 写入 KV，/data/kl8.txt 优先读 KV

name = "happy8"
compatibility_date = "2024-06-01"
pages_build_output_dir = "dist"

[[kv_namespaces]]
binding = "LOTTERY_KV"
id = "替换为你的 KV namespace id"
